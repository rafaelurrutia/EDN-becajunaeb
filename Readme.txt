Las carpetas "css", "font" y "js"  son de la libreria Bootstrap, no son necesarias incluirlas si Sitefinity ya lo esta usando.

Los CSS ha utilizar son style.css

La carpeta "fuentes" tiene la tipografia NOBEL, verificar ruta en el fontface.

Incluir carpeta de images y verificar rutas en el CSS y HTML.

resize-text.js es el JS que hace funcionar los botones de accesibilidad.
